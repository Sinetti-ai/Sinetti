# Schemas

The initial public set covers acceptance criteria, the synthetic customer-export
fixture, evidence, remedy, settlement, verification results, and arbitration
case records, each with a valid example and CI validation. Reputation is deferred
from this release. Identity references remain opaque protocol values; no private
identity database is part of these schemas.
